# 三模

- **Switch**：无加密。蓝牙（杰里）和 2.4G 接收器都走 GP2040-CE 的 Switch Pro 协议。
- **Xbox / PS**：Xbox One、PS5 走手柄 USB + CE 的 USB 引导认证。PS4 用编进固件的密钥（`serial.txt` / `sig.bin` / `key.pem`），不用网页上传。

脚位见 [CHIP_LOCK.md](CHIP_LOCK.md)。
