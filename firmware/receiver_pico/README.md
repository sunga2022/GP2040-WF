# 旧路径：RP2040 接收器

产品接收器已经改成 **CH32X035F8U6 + Si24R1**，见 [`../receiver_ch32/`](../receiver_ch32/) 和 `firmware-bin/GP2040-WF_receiver.bin`。

RP2040 是 7×7 mm 还要外挂 Flash，USB-A 键盘接收器塞不进 16.30×11.20 mm。这目录只留着对照，不要烧它的 UF2。
